name: Shellcheck
on: [pull_request, push]

jobs:
  shellcheck:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: apt-get install shellcheck
      - run: shellcheck config/.zshrc scripts/cleanup.bash scripts/setup-devtools.bash scripts/setup-shell.bash
